apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-redirect-ingress-org
  annotations:
    nginx.ingress.kubernetes.io/permanent-redirect: "https://www.connme.org"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  rules:
  - host: connme.org
  tls:
  - hosts:
    - connme.org
    secretName: https-secret-redirect-org
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress-org
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - www.connme.org
    secretName: https-secret-org
  rules:
  - host: www.connme.org
    http:
      paths:
      - pathType: Prefix
        path: /captcha
        backend:
          service:
            name: captcha-service
            port:
              number: 80
      - pathType: Prefix
        path: /images
        backend:
          service:
            name: images-service
            port:
              number: 80
      - pathType: Prefix
        path: /video
        backend:
          service:
            name: images-service
            port:
              number: 80
      - pathType: Prefix
        path: /helpdesk_ticket_attaches
        backend:
          service:
            name: images-service
            port:
              number: 80
      - pathType: Prefix
        path: /cgi-bin
        backend:
          service:
            name: app-service
            port:
              number: 80
      - pathType: Prefix
        path: /api/v1/domain
        backend:
          service:
            name: domain-name-service
            port:
              number: 80
      - pathType: Prefix
        path: /
        backend:
          service:
            name: ui-service
            port:
              number: 80

